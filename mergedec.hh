#ifndef MERGEDEC_HH
#define MERGEDEC_HH
 
// DEKLARACJE FUNKCJI

template <typename type> void sortowanie(type  tab[], type elemp, type elemo);
template <typename type> void polacz(type  tab[], type elemp, type elemsr, type elemo);
template <typename type> bool test(type tab[], type rozmiar);

#endif
